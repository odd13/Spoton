<%= form_for(@job) do |i| %>
        <p>
         <%= i.label :name %><br>
         <%= i.text_field :name %>
        </p>

        <p>
          <%= i.label :Description %><br>
          <%= i.text_field :description %>
        </p>


    <%= i.submit %>
<% end %>

